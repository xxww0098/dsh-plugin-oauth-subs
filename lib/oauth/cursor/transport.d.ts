/** Cursor AgentService lifecycle and OpenAI streaming translation. */
export declare function forwardCursor(response: any, { payload, cacheSessionId, stream, session, signal, runFn }: any): Promise<void>;
