node_modules
*.log
auth.json
proxy-key
fast-mode.json
models.json
.DS_Store
*.tsbuildinfo
.local/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Agent tooling: portable configs are tracked, local write metadata is not.
.amp/*.amp-write-meta
